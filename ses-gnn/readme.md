# SES-GNN: предсказание сайтов связывания

- `preprocess.py` — строит SES поверхность из PDB
- `build_graphs.py` — строит графы
- `train_clean.py` — обучает GNN (один GPU)
- `train_clean_ddp.py` — обучает GNN (несколько GPU, DDP)
- `evaluate.py` — оценка модели на тестовых данных с поддержкой фиксированного порога и проверки пересечений
- `viz-ppi.py` — визуализация предсказаний

## Установка

```bash
conda create -n ses-gnn python=3.10
conda activate ses-gnn
pip install -r requirements.txt
```

## Запуск

```bash
# 1. SES поверхность
python preprocess.py --dataset training_ppi.txt --pdb_dir ./pdb_files --out_dir ./data/raw

# 2. Графы
python build_graphs.py --raw_dir ./data/raw --out_dir ./data/graphs --r_cutoff 3.0 --max_neighbors 48 --sigma 3.0

# 3. Обучение (рекомендуемая конфигурация)
python train_clean_ddp.py \
    --data_dir ./data/graphs \
    --n_files 5000 \
    --n_val 50 \
    --layers 8 \
    --hidden 32 \
    --heads 4 \
    --lr 1e-3 \
    --epochs 200 \
    --val_interval 2 \
    --hard_ratio 2.0 \
    --ckpt_f1 best_f1.pt \
    --ckpt_roc best_roc.pt \
    --ckpt_pr best_pr.pt \
    --log_dir ./logs

# 4. Оценка модели на тесте
python evaluate.py \
    --model best_pr.pt \
    --data_dir ./data/graphs \
    --output_dir ./evaluation_results \
    --n_files 412 \
    --threshold 0.272 \
    --shuffle_seed 42 \
    --heads 4 \
    --train_val_size 5050

# 5. Визуализация
python viz-ppi.py --model best_model.pt --name 3P0C_D --threshold 0.4
```

## Параметры train_clean_ddp.py

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--data_dir` | `./data/graphs` | Папка с графами |
| `--n_files` | `3000` | Число файлов для обучения |
| `--n_val` | `50` | Число файлов для валидации |
| `--threshold` | `4.0` | Порог интерфейса (Å) |
| `--layers` | `6` | Слоёв TransformerConv |
| `--hidden` | `32` | Размер скрытого слоя |
| `--heads` | `4` | Число голов внимания |
| `--lr` | `1e-3` | Learning rate |
| `--epochs` | `300` | Количество эпох |
| `--val_interval` | `5` | Интервал валидации (эпох) |
| `--hard_ratio` | `2.0` | Hard negative mining ratio |
| `--ckpt_f1` | `best_f1.pt` | Чекпоинт по F1 |
| `--ckpt_roc` | `best_roc.pt` | Чекпоинт по ROC‑AUC |
| `--ckpt_pr` | `best_pr.pt` | Чекпоинт по PR‑AUC |
| `--resume` | `None` | Путь к чекпоинту для продолжения |
| `--checkpoint` | `True` | Gradient checkpointing |
| `--log_dir` | `./logs` | Папка для логов |

## 📊 Результаты всех экспериментов

### Конфигурации

| # | Эксперимент | Графы | n_files | Слоёв | Hidden | Heads | hard_ratio | Val ROC‑AUC (best) | Test Per‑protein F1 | Test Global ROC‑AUC | Test Global PR‑AUC |
|---|-------------|-------|---------|-------|--------|-------|------------|--------------------|--------------------|--------------------|--------------------|
| 1 | Базовый (старые графы) | R=2, N=24, σ=1.5 | 1500 | 6 | 64 | 4 | 3.0 | 0.706 | 0.454 | 0.672 | — |
| 2 | Эксп. 3 (новые графы) | R=3, N=48, σ=3.0 | 3000 | 6 | 32 | 4 | 3.0 | 0.767 | 0.452 | 0.725 | — |
| 3 | 6 слоёв, hard=2.0 | R=3, N=48, σ=3.0 | 5000 | 6 | 32 | 4 | 2.0 | 0.781 | 0.528 | 0.789 | 0.552 |
| 4 | 6 слоёв, hard=1.5 | R=3, N=48, σ=3.0 | 5000 | 6 | 32 | 4 | 1.5 | 0.778 | — | — | — |
| 5 | 8 слоёв, h=16, heads=4 | R=3, N=48, σ=3.0 | 1000 | 8 | 16 | 4 | 2.0 | 0.687 | — | — | — |
| 6 | 8 слоёв, h=32, heads=2 | R=3, N=48, σ=3.0 | 1000 | 8 | 32 | 2 | 2.0 | 0.695 | — | — | — |
| 7 | **8 слоёв, best_f1** | R=3, N=48, σ=3.0 | **5000** | **8** | **32** | **4** | **2.0** | 0.8069 | **0.540** | 0.806 | 0.580 |
| 8 | **8 слоёв, best_pr** | R=3, N=48, σ=3.0 | **5000** | **8** | **32** | **4** | **2.0** | 0.8068 | 0.537 | **0.808** | **0.583** |
| 9 | **8 слоёв, checkpoint 200** | R=3, N=48, σ=3.0 | **5000** | **8** | **32** | **4** | **2.0** | — | 0.512 | 0.803 | 0.579 |
| 10 | Мультимасштабные графы | **в процессе** | — | — | — | — | — | — | — | — | — |

### Сравнение с dMaSIF

| Метрика | dMaSIF | Эксп. 7 (best_f1) | Отставание | Эксп. 8 (best_pr) | Отставание |
|---------|--------|-------------------|------------|-------------------|------------|
| **ROC‑AUC** | 0.850–0.870 | **0.806** | -0.044 | **0.808** | -0.042 |
| **PR‑AUC** | 0.500–0.600 | **0.580** | **+0.080** | **0.583** | **+0.083** |
| **F1** | ~0.450 | **0.540** | **+0.090** | **0.537** | **+0.087** |
| **Recall** | ~0.500 | **0.713** | **+0.213** | **0.700** | **+0.200** |
| **Precision** | ~0.500 | 0.481 | -0.019 | 0.494 | -0.006 |

**Итог:**  
- Мы **превзошли dMaSIF по F1, Recall и PR‑AUC**.  
- Отстаём по ROC‑AUC на **0.042–0.044** — это основная цель для следующих экспериментов.
- **Лучшая модель по ROC‑AUC** — `best_pr_8x32h4.pt` (0.808).
- **Лучшая модель по F1** — `best_f1_8x32h4.pt` (0.540).

**Наши преимущества:**  
- Инференс быстрее (оценочно в 5 раз)  
- Памяти меньше (оценочно в 7 раз)  
- Не нужны предварительные вычисления (меши, фичи)  
- Сквозной пайплайн от PDB до предсказания

---

## 🔬 План экспериментов (что дальше)

### Эксперимент 1: Увеличение глубины до 10 слоёв (приоритет 1)

**Гипотеза:** ещё больше слоёв может дать дополнительный прирост ROC‑AUC.

```bash
python train_clean_ddp.py \
    --data_dir ./data/graphs \
    --n_files 5000 \
    --n_val 50 \
    --layers 10 \
    --hidden 32 \
    --heads 4 \
    --lr 1e-3 \
    --epochs 200 \
    --val_interval 2 \
    --resume best_pr_8x32h4.pt \
    --ckpt_roc best_roc_10x32h4.pt \
    --ckpt_f1 best_f1_10x32h4.pt \
    --ckpt_pr best_pr_10x32h4.pt \
    --log_dir ./logs_10x32h4
```

**Ожидаемый прирост:** ROC‑AUC +0.005–0.010

---

### Эксперимент 2: Мультимасштабные графы (приоритет 2)

**Гипотеза:** рёбра на 2Å, 4Å и 8Å с отбором центроидов дадут глобальный контекст.

**Статус:** графы формируются.

**Ожидаемый прирост:** ROC‑AUC +0.030–0.050

---

### Эксперимент 3: Глобальный контекст через пулинг (приоритет 3)

**Гипотеза:** добавление глобальной информации о всей поверхности улучшит разделение.

**Ожидаемый прирост:** ROC‑AUC +0.020–0.030

---

### Эксперимент 4: Добавить признаки из dMaSIF (приоритет 4)

**Гипотеза:** электростатика, гидрофобность, кривизна помогут.

**Ожидаемый прирост:** ROC‑AUC +0.040–0.070

---

### Эксперимент 5: Ансамбль моделей (приоритет 5)

**Гипотеза:** усреднение 3–5 моделей снизит дисперсию.

**Ожидаемый прирост:** F1 +0.020–0.030

---

### Эксперимент 6: Увеличить модель (приоритет 6)

**Гипотеза:** hidden=64–128 могут дать прирост (если память позволит).

**Ожидаемый прирост:** ROC‑AUC +0.020–0.040

---

## 📋 Приоритеты экспериментов

| # | Эксперимент | Сложность | Ожидаемый прирост | Когда делать |
|---|-------------|-----------|-------------------|--------------|
| 1 | **10 слоёв** | Низкая | Низкий | **Сейчас** |
| 2 | Мультимасштабные графы | Средняя | **Высокий** | После #1 |
| 3 | Глобальный контекст | Низкая | Средний | После #2 |
| 4 | Добавить признаки | Высокая | Высокий | После #2–#3 |
| 5 | Ансамбль | Низкая | Низкий | В конце |
| 6 | Увеличить модель | Средняя | Средний | После #4 |

## 🎯 Целевые метрики

Конечная цель — превзойти dMaSIF по ROC‑AUC (0.85+):

| Метрика | Текущий (8 слоёв) | Цель |
|---------|-------------------|------|
| ROC‑AUC | 0.808 | **0.82+** |
| PR‑AUC | 0.583 | **0.60+** |
| Per‑protein F1 | 0.540 | **0.55+** |
| Recall | 0.713 | **0.75+** |

---

## 🛠️ Технические детали

### Оценка модели (evaluate.py)

- **Автоопределение архитектуры:** определяется `hidden`, `layers`, `heads` из чекпоинта (можно задать вручную).
- **Фиксированный порог:** `--threshold` для воспроизводимых результатов.
- **Проверка пересечений:** автоматическая проверка, что тестовые файлы не пересекаются с `train_files.txt`.
- **Тестовый сплит:** `--shuffle_seed 42` для согласованности с обучением.

### Воспроизводимость

При обучении `train_clean_ddp.py` сохраняет в `--log_dir`:
- `train_files.txt` — список файлов для обучения
- Полный CSV‑лог метрик
- Периодические чекпоинты (каждые 25 эпох)

### Веса моделей

| Модель | Файл | Per‑protein F1 | Global ROC‑AUC |
|--------|------|----------------|----------------|
| Лучшая по F1 | `best_f1_8x32h4.pt` | **0.540** | 0.806 |
| Лучшая по ROC‑AUC | `best_pr_8x32h4.pt` | 0.537 | **0.808** |

